# ########################
# # FRONTEND (main domain)
# ########################
# server {
#     listen 443 ssl;
#     server_name recallcode.test;

#     ssl_certificate     /etc/nginx/ssl/server.crt;
#     ssl_certificate_key /etc/nginx/ssl/server.key;

#     location / {
#         proxy_pass http://client;  # frontend container
#     }
# }

# ########################
# # FRONTEND (sub domain for docs)
# ########################
# server {
#     listen 443 ssl;
#     server_name docs.recallcode.test;

#     ssl_certificate     /etc/nginx/ssl/server.crt;
#     ssl_certificate_key /etc/nginx/ssl/server.key;

#     location / {
#         proxy_pass http://docs;  # frontend container
#     }
# }
# ########################
# # BACKEND (api subdomain)
# ########################
# server {
#     listen 443 ssl;
#     server_name api.recallcode.test;

#     ssl_certificate     /etc/nginx/ssl/server.crt;
#     ssl_certificate_key /etc/nginx/ssl/server.key;

#     # Proxy all backend requests
#     location / {
#         proxy_pass http://backend:7000;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # WebSocket support (if needed)
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
# }

# ########################
# # HTTP → HTTPS redirect
# ########################
# server {
#     listen 80;
#     server_name recallcode.test docs.recallcode.test api.recallcode.test ;
#     return 301 https://$host$request_uri;
# }



########################
# FRONTEND (main domain)
########################
server {
    listen 443 ssl;
    server_name recallcode.test;

    ssl_certificate     /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    # Static long-cache
    location ~* ^/(assets|static|fonts)/ {
        proxy_pass http://client;
        add_header Cache-Control "public, max-age=31536000, immutable";
        expires 365d;
    }

    # App root
    location / {
        proxy_pass http://client;
        add_header Cache-Control "no-store";
    }
}

########################
# DOCS (subdomain)
########################
server {
    listen 443 ssl;
    server_name docs.recallcode.test;

    ssl_certificate     /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    location / {
        proxy_pass http://docs;
        add_header Cache-Control "no-store";
    }
}

########################
# API (backend)
########################
server {
    listen 443 ssl;
    server_name api.recallcode.test;

    ssl_certificate     /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    # Rate limit API
    location / {
        limit_req zone=api_limit burst=20 nodelay;

        proxy_pass http://backend:7000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSockets
    location /ws/ {
        proxy_pass http://backend:7000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
    }
}

########################
# HTTP → HTTPS Redirect
########################
server {
    listen 80;
    server_name recallcode.test docs.recallcode.test api.recallcode.test;
    return 301 https://$host$request_uri;
}
